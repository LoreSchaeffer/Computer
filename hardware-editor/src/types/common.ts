export type Variant = 'primary' | 'secondary' | 'success' | 'warning' | 'error';

export type BackgroundVariant = 'primary' | 'secondary' | 'tertiary';

export type Position = {
    x: number;
    y: number;
};